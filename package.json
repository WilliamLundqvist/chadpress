{
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "setup": "pnpm install",
    "build": "turbo run build",
    "build:css": "pnpm --filter @repo/ui build:css",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
